<p>Listing <%= @packages_count %> packages.</p>

<table>
  <%= for package <- @packages do %>
    <tr>
      <td><a href="<%= hex_url(package) %>"><%= package.name %></a></dt>
      <td><%= package.description %></td>
    </tr>
  <% end %>
</table>
